# نظام التحقق من الهوية (Signup Verification Flow)

تم إضافة نظام تحقق مكون من 6 أرقام عند تسجيل مستخدم جديد لضمان أمان الحسابات والتأكد من بيانات التواصل.

## 🛠️ كيف تمت الإضافة (التفاصيل التقنية)

### 1. طبقة البيانات والمصادقة (Auth Service)
*   **تعديل الـ Schema**: تم إضافة حقول `phone`, `isVerified`, `verificationCode`, و `verificationMethod` لموديل المستخدم في MongoDB.
*   **توليد الكود**: عند استدعاء `/register` يتم توليد كود عشوائي:
    ```javascript
    const code = Math.floor(100000 + Math.random() * 900000).toString();
    ```
*   **التزامن العابر للخدمات (Kafka)**: بمجرد الحفظ في قاعدة البيانات، ترسل خدمة الـ Auth حدث `USER_REGISTERED` عبر Kafka يحتوي على بيانات المستخدم والكود.

### 2. طبقة التنبيهات (Notification Service)
*   تم برمجة هذه الخدمة لتكون "Consumer" لموضوع (Topic) الـ `auth-events`.
*   بمجرد استلام الكود، تقوم الخدمة بمحاكاة الإرسال (Logging) بناءً على الوسيلة المختارة (Email أو Phone).

### 3. الواجهة الأمامية (Frontend & UI)
*   **تحديث الـ Signup Modal**: تم إضافة حقل رقم الهاتف وقائمة اختيار وسيلة التحقق.
*   **نافذة التحقق (Verification Modal)**: تم إنشاء واجهة جديدة تماماً تظهر بعد الضغط على "Join" تطلب من المستخدم إدخال الكود.
*   **الربط البرمجي (app.js & db.js)**:
    *   تم إضافة دالة `db.verifyCode` للتواصل مع الـ Backend.
    *   تم تعديل `handleSignup` لتخزين الإيميل مؤقتاً في `window.pendingEmail` حتى تتم عملية التحقق.

## 📝 كيفية الاختبار
1. قم بالتسجيل في التطبيق.
2. سيتم تحويلك تلقائياً لصفحة التحقق.
3. ابحث عن الكود في الـ Logs الخاصة بـ `notification-service`:
   ```bash
   kubectl logs -l app=notification-service -n mysakn-app
   ```
4. أدخل الكود المكون من 6 أرقام لإتمام العملية.
