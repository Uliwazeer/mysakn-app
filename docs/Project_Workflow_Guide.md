# 📘 دليل عمل المشروع (Project Workflow Guide)

هذا الملف يشرح كيفية التعامل مع المشروع بعد التحديثات الأخيرة (Cloud & Local Support).

## 1. الهيكل الجديد (New Structure)
تم تنظيم المشروع ليكون جاهزاً للعمل المؤسسي (Enterprise Grade):
*   **`apps/`**: الكود الأساسي (Frontend & Backend).
*   **`infra/`**:
    *   **`terraform/`**: أكواد بناء البنية التحتية على AWS (EKS, VPC, Jenkins).
    *   **`k8s/`**: ملفات Kubernetes Manifests.
*   **`scripts/`**:
    *   **`deploy.sh`**: الاسكريبت الرئيسي للتشغيل.
    *   **`tools.sh`**: اسكريبت تسطيب الأدوات (Prometheus, Grafana, ArgoCD, ELK).

---

## 2. خطوات التشغيل (How to Run)

### أولاً: تجهيز الأدوات (Tools Setup)
قبل تشغيل التطبيق، يفضل تسطيب أدوات المراقبة والـ Logging.
شغل الأمر ده:
```bash
./scripts/tools.sh
```
هيطلعلك قائمة تختار منها:
1.  **Monitoring**: يسطب Prometheus & Grafana لمراقبة الـ Metrics.
2.  **ArgoCD**: لعمل GitOps Deployments.
3.  **ELK Stack**: (Elasticsearch, Logstash, Kibana) عشان الـ Logs.

### ثانياً: تشغيل التطبيق (Deployment)
شغل الأمر ده:
```bash
./scripts/deploy.sh
```
هيسألك سؤالين:

**1. Select Deployment Environment:**
*   اختيار `1) Local (Minikube)`: هيشتغل على جهازك (زي ما كنا شغالين).
*   اختيار `2) Cloud (AWS EKS)`:
    *   هيطلب منك `AWS Access Key` و `Secret Key`.
    *   هيربط مع الـ EKS Cluster بتاعك.
    *   هيعمل Deploy هناك.

**2. Cleanup Prompt (Local Only):**
*   هيسألك لو عايز تمسح الديبلويمينت القديم وتبدأ على نضافة (`clean slate`).

---

## 3. البنية التحتية (Infrastructure Provisioning)
لو عايز تبني السيرفرات من الصفر على AWS:
1.  ادخل فولدر `infra/terraform`.
2.  عدل ملف `variables.tf` لو محتاج.
3.  شغل:
    ```bash
    terraform init
    terraform apply
    ```
    *   ده هيعملك **EKS Cluster** جاهز.
    *   وهيعملك **Jenkins Server** جاهز.

---

## 4. الروابط المهمة (Access Links)
بعد التشغيل (Local)، دي الروابط المتوقعة:
*   **Frontend**: `http://<MINIKUBE_IP>:30080`
*   **Grafana**: `http://<MINIKUBE_IP>:3??` (حسب الـ Port اللي هيطلع من الـ Script).
*   **ArgoCD**: `https://<MINIKUBE_IP>:3??`
*   **Kibana**: `http://<MINIKUBE_IP>:5601` (لو سطبت ELK).

---
**ملحوظة:** المشروع دلوقتي جاهز يترفع على الريبو بتاعك: `https://github.com/Uliwazeer/mysakn`
